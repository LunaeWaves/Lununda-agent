apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lununda.fullname" . }}-config
  labels:
    {{- include "lununda.labels" . | nindent 4 }}
data:
  LUNUNDA_PORT: {{ .Values.gateway.port | quote }}
  LUNUNDA_BIND: "all"
  LUNUNDA_STORAGE_TYPE: "postgres"
  LUNUNDA_STORAGE_AUTO_MIGRATE: "true"
  {{- with .Values.timezone }}
  ## Server default timezone — last fallback after chatter / agent /
  ## system prefs.
  TZ: {{ . | quote }}
  {{- end }}

  ## Object store
  LUNUNDA_OBJECT_STORE_TYPE: {{ .Values.objectStore.type | quote }}
  {{- with .Values.objectStore.bucket }}
  LUNUNDA_OBJECT_STORE_BUCKET: {{ . | quote }}
  {{- end }}
  {{- with .Values.objectStore.region }}
  LUNUNDA_OBJECT_STORE_REGION: {{ . | quote }}
  {{- end }}
  {{- with .Values.objectStore.endpoint }}
  LUNUNDA_OBJECT_STORE_ENDPOINT: {{ . | quote }}
  {{- end }}
  {{- with .Values.objectStore.accountId }}
  LUNUNDA_OBJECT_STORE_ACCOUNTID: {{ . | quote }}
  {{- end }}
  {{- with .Values.objectStore.prefix }}
  LUNUNDA_OBJECT_STORE_PREFIX: {{ . | quote }}
  {{- end }}
  LUNUNDA_OBJECT_STORE_USESSL: {{ .Values.objectStore.useSSL | quote }}
  {{- if .Values.objectStore.aliyunInternal }}
  LUNUNDA_OBJECT_STORE_ALIYUN_INTERNAL: "true"
  {{- end }}

  ## Sandbox
  {{- if .Values.sandbox.enabled }}
  LUNUNDA_SANDBOX_BACKEND: {{ .Values.sandbox.backend | quote }}
  {{- with .Values.sandbox.image }}
  LUNUNDA_SANDBOX_IMAGE: {{ . | quote }}
  {{- end }}
  {{- end }}

  {{- with .Values.extraEnv }}
  {{- range $k, $v := . }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
  {{- end }}
